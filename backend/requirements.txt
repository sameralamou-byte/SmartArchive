fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (last stable release) auto-detects bcrypt's backend using an
# API that bcrypt>=4.1 changed, causing spurious "password cannot be longer
# than 72 bytes" errors even for short passwords. Pin to a known-compatible
# bcrypt release until passlib cuts a new release (or we switch to a
# maintained wrapper) that supports modern bcrypt properly.
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.8
celery==5.4.0
minio==7.2.8
httpx==0.27.2
structlog==24.4.0
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.9
pytest-cov==5.0.0
aiosqlite==0.20.0
prometheus-client==0.21.0
slowapi==0.1.9
pillow==10.4.0
pypdfium2==4.30.0
paddleocr==3.3.1
paddlepaddle==3.3.0
